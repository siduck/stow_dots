(defpoll datetime :interval "1s" "date '+%a %e | %H:%M'")

(defwidget time []
  (label :text datetime :class 'txt datetime'))

(defwidget middle []
  (box :halign "end" :space-evenly false
    (time)))
